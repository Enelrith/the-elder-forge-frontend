export interface RegisterUser {
  email: string;
  password: string;
}

export interface User {
  id: string;
  createdAt: string;
  email: string;
}

export interface Auth {
  email: string;
  password: string;
}
